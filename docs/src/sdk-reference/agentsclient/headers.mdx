---
title: "headers"
description: "Return HTTP headers for authenticated API requests"
---

Constructs and returns a dictionary containing the 'Authorization' header, which is formatted as a Bearer token using the API key stored in self.token


## Returns

`Dict`[`str`, `str`]
