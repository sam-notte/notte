---
title: "NottePersona"
description: "Self-service identities for web automation (account creation, 2FA,etc"
---

).

Notte Personas provide automated identity management for AI agents, enabling them to create accounts, handle two-factor authentication, and interact with web platforms without manual intervention.

Notte Personas come with complete digital identities:
- Unique Email Address: Dedicated mailbox for each persona with full email management
- Phone Number: SMS-capable phone number for verification and 2FA
- Credential Vault: Optional secure storage for passwords and authentication tokens
- Automated Communication: Built-in email and SMS reading capabilities
- 2FA Support: Seamless handling of two-factor authentication flows

## Methods

### aemails

```python
aemails(data: *<class 'notte_sdk.types.MessageReadRequestDict'>) -> collections.abc.Sequence[notte_sdk.types.EmailResponse]
```

**Returns:**

`Sequence`[[`EmailResponse`](/sdk-reference/misc/emailresponse)]

---

### asms

```python
asms(data: *<class 'notte_sdk.types.MessageReadRequestDict'>) -> collections.abc.Sequence[notte_sdk.types.SMSResponse]
```

**Returns:**

`Sequence`[[`SMSResponse`](/sdk-reference/misc/smsresponse)]

---

### create

```python
create()
```

---

### create_number

```python
create_number(data: *<class 'notte_sdk.types.CreatePhoneNumberRequestDict'>) -> <class 'notte_sdk.types.CreatePhoneNumberResponse'>
```

Create a phone number to the persona

**Returns:**

[`CreatePhoneNumberResponse`](/sdk-reference/misc/createphonenumberresponse)

---

### delete

```python
delete()
```

Delete the persona from the notte console

---

### delete_number

```python
delete_number() -> <class 'notte_sdk.types.DeletePhoneNumberResponse'>
```

Delete the phone number from the persona

**Returns:**

[`DeletePhoneNumberResponse`](/sdk-reference/misc/deletephonenumberresponse)

---

### emails

```python
emails(data: *<class 'notte_sdk.types.MessageReadRequestDict'>) -> collections.abc.Sequence[notte_sdk.types.EmailResponse]
```

Read recent emails sent to the persona

**Returns:**

`Sequence`[[`EmailResponse`](/sdk-reference/misc/emailresponse)]

---

### reset

```python
reset()
```

Resets the resource to its initial state

---

### sms

```python
sms(data: *<class 'notte_sdk.types.MessageReadRequestDict'>) -> collections.abc.Sequence[notte_sdk.types.SMSResponse]
```

Read recent sms messages sent to the persona

**Returns:**

`Sequence`[[`SMSResponse`](/sdk-reference/misc/smsresponse)]

---

### start

```python
start()
```

---

### stop

```python
stop()
```

---


## Inheritance

Inherits from: SyncResource, BasePersona


## Module

`notte_sdk.endpoints.personas`
