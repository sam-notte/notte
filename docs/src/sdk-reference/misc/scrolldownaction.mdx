---
title: "ScrollDownAction"
description: "Scroll down by a given amount of pixels"
---

Use `null` for scrolling down one page.

**Example:**
```python
from notte_core.actions import ScrollDownAction
# Dict syntax
session.execute({"type": "scroll_down", "amount": 500})  # Scroll down 500 pixels
session.execute({"type": "scroll_down"})  # Scroll down one page
# Pydantic syntax
session.execute(ScrollDownAction(amount=500))
session.execute(ScrollDownAction(amount=None))
```

## Fields

<ParamField path="type" type="Literal['scroll_down']" required>
</ParamField>

<ParamField path="category" type="str" required>
</ParamField>

<ParamField path="description" type="str" required>
</ParamField>

<ParamField path="amount" type="UnionType[int, None]" required>
</ParamField>


## Module

`notte_core.actions`
